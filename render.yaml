services:
  - type: web
    name: candidate-onboarding-backend
    env: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:sNE@R+P8JPPg.Cc@db.bbwrosuuksxzvrstnctp.supabase.co:5432/postgres?schema=public
